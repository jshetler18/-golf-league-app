v13.111 — Desktop Responsive Layout

- Keeps the existing phone layout unchanged.
- Adds a tablet-to-desktop responsive progression with a dedicated wide-screen presentation above 1180px.
- Home screen becomes a polished 2-column desktop dashboard (3 columns on very wide screens) while retaining the same Golf Sim icons, colors, profile menu, alerts, and bottom navigation.
- Desktop inner pages use a wider centered content area, larger cards, improved spacing, wider tables, and multi-column layouts where useful.
- Teams, booking rows, messages, profile editing, and admin team cards take advantage of desktop width without changing app functionality.
- Desktop navigation receives a more polished sticky header treatment.
- No database changes required.
