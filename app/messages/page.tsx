'use client'
import {useEffect,useState} from 'react'; import {supabase} from '@/lib/supabase'; import {PlayerPage} from '@/components/PlayerMobileChrome'
export default function Messages(){const [rows,setRows]=useState<any[]>([]),[read,setRead]=useState<Set<string>>(new Set());
async function load(){const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {data:a}=await supabase.from('announcements').select('*').or(`expires_at.is.null,expires_at.gt.${new Date().toISOString()}`).order('is_pinned',{ascending:false}).order('created_at',{ascending:false});const {data:r}=await supabase.from('announcement_reads').select('announcement_id').eq('user_id',user.id);setRows(a||[]);setRead(new Set((r||[]).map(x=>x.announcement_id)))}
useEffect(()=>{load()},[]);async function mark(id:string){const {data:{user}}=await supabase.auth.getUser();if(!user)return;await supabase.from('announcement_reads').upsert({announcement_id:id,user_id:user.id});setRead(new Set([...read,id]));window.dispatchEvent(new Event('league-unread-changed'))}
return <PlayerPage title="Messages"><div className="simple-mobile-page"><h1>Messages</h1><p className="muted">League messages and announcements.</p><div className="message-list">{rows.length?rows.map(m=><button key={m.id} onClick={()=>mark(m.id)} className={'message-card '+(!read.has(m.id)?'unread':'')}><div><strong>{m.title}</strong>{!read.has(m.id)&&<span className="new-dot">New</span>}</div><p>{m.body}</p><small>{new Date(m.created_at).toLocaleDateString()}</small></button>):<div className="card">No messages yet.</div>}</div></div></PlayerPage>}
