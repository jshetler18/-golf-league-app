'use client'
import {useState} from 'react';import {supabase} from '@/lib/supabase'
export default function Settings(){const [pw,setPw]=useState(''),[msg,setMsg]=useState('');async function save(){if(pw.length<6){setMsg('Password must be at least 6 characters.');return}const {error}=await supabase.auth.updateUser({password:pw});setMsg(error?error.message:'Password updated.');if(!error)setPw('')};return <div className="simple-mobile-page"><h1>Settings</h1><div className="card"><h2>Account</h2><label>New Password<input type="password" value={pw} onChange={e=>setPw(e.target.value)} placeholder="Enter a new password"/></label><button className="btn" onClick={save}>Update Password</button>{msg&&<p>{msg}</p>}</div></div>}
