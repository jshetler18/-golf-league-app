'use client'
import {useEffect,useState} from 'react'; import {supabase} from '@/lib/supabase'; import {PlayerPage} from '@/components/PlayerMobileChrome'
export default function Profile(){const [name,setName]=useState(''),[avatar,setAvatar]=useState<string|null>(null),[msg,setMsg]=useState('');
useEffect(()=>{(async()=>{const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {data}=await supabase.from('profiles').select('full_name,avatar_url').eq('id',user.id).single();setName(data?.full_name||'');setAvatar(data?.avatar_url||null)})()},[])
async function upload(e:any){const f=e.target.files?.[0];if(!f)return;const {data:{user}}=await supabase.auth.getUser();if(!user)return;const ext=f.name.split('.').pop();const path=`${user.id}/profile.${ext}`;const {error}=await supabase.storage.from('avatars').upload(path,f,{upsert:true});if(error){setMsg(error.message);return}const {data}=supabase.storage.from('avatars').getPublicUrl(path);setAvatar(data.publicUrl+'?v='+Date.now())}
async function save(){const {error}=await supabase.rpc('update_my_profile',{p_full_name:name,p_avatar_url:avatar});setMsg(error?error.message:'Profile saved.')}
return <PlayerPage title="My Profile"><div className="simple-mobile-page"><h1>My Profile</h1><div className="profile-editor"><div className="avatar-preview">{avatar?<img src={avatar}/>:<span>👤</span>}</div><label className="btn secondary">Choose Profile Photo<input hidden type="file" accept="image/png,image/jpeg,image/webp" onChange={upload}/></label><label>Display Name<input value={name} onChange={e=>setName(e.target.value)}/></label><button className="btn" onClick={save}>Save Profile</button>{msg&&<p>{msg}</p>}</div></div></PlayerPage>}
