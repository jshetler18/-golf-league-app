v13.135 — Admin navigation cleanup
- On all /admin routes, clicking the Golf League logo now returns to /admin instead of the player home page.
- Removed the legacy Admin Home back-button styling by forcing .admin-back-v1237 hidden.
- Removed the “Administration” eyebrow text from the shared admin header and each admin page heading.
