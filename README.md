Golf League App v13.130 Update Only

Changes:
- Removes the Admin Home back button from admin pages when they are loaded inside the Admin Home workspace.
- Embedded admin pages now auto-size to their full content height.
- Removes the second/nested scrollbar so the browser page uses one main scrollbar.

Replace the included files in the same paths, then redeploy.
